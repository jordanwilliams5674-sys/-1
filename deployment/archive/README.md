# Archived deployment configs

These files are kept for reference only.

Current deployment path:

- Platform: Cloudflare Pages
- Output directory: `docs`
- Static entry: `docs/index.html`
- Static data: `docs/api/webdata.json`

Do not use `render.yaml` for the current deployment. Render asked for a payment card during setup, so the active path moved to Cloudflare Pages.
